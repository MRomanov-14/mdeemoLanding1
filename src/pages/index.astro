---
import Layout from '../layouts/Layout.astro';
---

<Layout title="CLICK Logística | Reclutamiento y Posicionamiento de Personal" description="Click Logística: Expertos en reclutamiento y posicionamiento de personal. Optimizamos tu operación con talento humano especializado.">
    <!-- Hero Section con Canvas Interactivo -->
    <section class="hero-critical relative h-screen flex items-center justify-center overflow-hidden bg-brand-black">
        <canvas id="networkCanvas" class="hero-canvas"></canvas>

        <!-- Radial Gradient Overlay -->
        <div
            class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-brand-primary/10 via-brand-black/50 to-brand-black pointer-events-none">
        </div>

        <div class="relative z-10 max-w-7xl mx-auto px-6 text-center">
            <div
                class="hidden md:inline-flex items-center gap-2 px-3 py-1.5 md:px-4 md:py-2 rounded-full bg-white/5 border border-white/10 text-brand-primary text-xs md:text-sm font-semibold mb-8 animate-float backdrop-blur-md">
                <span class="w-1.5 h-1.5 md:w-2 md:h-2 rounded-full bg-brand-primary animate-pulse"></span>
                Tecnología aplicada a Operaciones
            </div>

            <h1
                class="h1-critical text-5xl md:text-8xl font-black tracking-tight text-white mb-8 leading-tight font-[Outfit]">
                IMPULSAMOS TU<br />
                <span
                    class="text-transparent bg-clip-text bg-gradient-to-r from-brand-primary via-yellow-200 to-brand-primary animate-pulse-slow">OPERACIÓN</span>
            </h1>

            <p class="p-critical mt-4 max-w-2xl mx-auto text-xl text-gray-400 font-light leading-relaxed">
                Somos el motor oculto detrás de las grandes marcas. Conectamos <span
                    class="text-white font-medium">eficiencia operativa</span> con <span
                    class="text-white font-medium">capital humano de élite</span>.
            </p>

            <div class="mt-12 flex flex-col sm:flex-row gap-6 justify-center items-center">
                <button onclick="document.getElementById('reclutamiento').scrollIntoView({behavior: 'smooth'})"
                    class="group relative px-10 py-5 bg-brand-primary text-black rounded-full font-bold text-lg shadow-[0_0_30px_rgba(234,179,8,0.3)] hover:shadow-[0_0_50px_rgba(234,179,8,0.5)] transition-all overflow-hidden min-w-[250px]">
                    <div
                        class="absolute inset-0 w-full h-full bg-gradient-to-r from-transparent via-white/40 to-transparent -translate-x-full group-hover:animate-[shimmer_1.5s_infinite]">
                    </div>
                    <span>Soy Talento</span>
                    <span class="block text-xs font-normal opacity-80 mt-1">Busco Trabajo</span>
                    <!-- <i class="fa-solid fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i> -->
                </button>
                <a href="/empresas"
                    class="group relative px-10 py-5 bg-white text-black border border-white/20 rounded-full font-bold text-lg hover:bg-gray-200 transition-all backdrop-blur-sm min-w-[250px] flex flex-col items-center justify-center">
                    <span>Soy Empresa</span>
                    <span class="block text-xs font-normal opacity-70 mt-1 text-black">Busco Personal</span>
                </a>
            </div>
        </div>

        <!-- Scroll Indicator -->
        <div class="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-bounce text-brand-primary cursor-pointer z-20" onclick="document.getElementById('ecosistema').scrollIntoView({behavior: 'smooth'})">
            <i class="fa-solid fa-chevron-down text-2xl"></i>
        </div>
    </section>

    <!-- Ticker de Clientes (Ecosistema) -->
    <div class="bg-black border-y border-white/10 py-8 overflow-hidden whitespace-nowrap relative">
        <div class="absolute inset-y-0 left-0 w-32 bg-gradient-to-r from-black to-transparent z-10"></div>
        <div class="absolute inset-y-0 right-0 w-32 bg-gradient-to-l from-black to-transparent z-10"></div>

        <div class="inline-block animate-[scroll-left_40s_linear_infinite]" id="ticker-content">
            <!-- Injected via JS -->
        </div>
    </div>

    <!-- Sección de Logros y Ecosistema -->
    <section id="ecosistema" class="py-32 bg-brand-black relative">
        <div class="max-w-7xl mx-auto px-6 lg:px-8">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-20 items-center">
                <!-- Columna Texto -->
                <div class="space-y-10">
                    <div class="space-y-4">
                        <h2 class="text-4xl md:text-6xl font-black text-white leading-tight font-[Outfit]">
                            MÁS QUE STAFFING,<br>SOMOS <span class="text-brand-primary">INGENIERÍA OPERATIVA</span>.
                        </h2>
                        <div class="h-1 w-20 bg-brand-primary rounded-full"></div>
                    </div>

                    <p class="text-xl text-gray-400 leading-relaxed font-light">
                        En CLICK Logística no solo cubrimos vacantes. Analizamos, optimizamos y desplegamos células
                        operativas de alto rendimiento para gigantes de la industria.
                    </p>
                    <!-- Iconos de Engagement -->
                    <div class="grid grid-cols-3 gap-6 pt-6 border-t border-white/10 mt-8">
                        <div class="text-center group cursor-default">
                            <div class="w-14 h-14 mx-auto bg-white/5 rounded-2xl flex items-center justify-center text-brand-primary mb-3 border border-white/10 group-hover:bg-brand-primary group-hover:text-black group-hover:scale-110 transition-all duration-300 shadow-[0_0_20px_rgba(0,0,0,0.3)]">
                                <i class="fa-solid fa-rocket text-2xl"></i>
                            </div>
                            <p class="text-xs font-bold text-gray-300 uppercase tracking-wider group-hover:text-white transition-colors">Agilidad</p>
                        </div>
                        <div class="text-center group cursor-default">
                            <div class="w-14 h-14 mx-auto bg-white/5 rounded-2xl flex items-center justify-center text-brand-primary mb-3 border border-white/10 group-hover:bg-brand-primary group-hover:text-black group-hover:scale-110 transition-all duration-300 shadow-[0_0_20px_rgba(0,0,0,0.3)]">
                                <i class="fa-solid fa-user-shield text-2xl"></i>
                            </div>
                            <p class="text-xs font-bold text-gray-300 uppercase tracking-wider group-hover:text-white transition-colors">Confianza</p>
                        </div>
                        <div class="text-center group cursor-default">
                            <div class="w-14 h-14 mx-auto bg-white/5 rounded-2xl flex items-center justify-center text-brand-primary mb-3 border border-white/10 group-hover:bg-brand-primary group-hover:text-black group-hover:scale-110 transition-all duration-300 shadow-[0_0_20px_rgba(0,0,0,0.3)]">
                                <i class="fa-solid fa-chart-pie text-2xl"></i>
                            </div>
                            <p class="text-xs font-bold text-gray-300 uppercase tracking-wider group-hover:text-white transition-colors">Resultados</p>
                        </div>
                    </div>
                </div>

                <!-- Columna Visual (Carousel Premium) -->
                <div class="relative w-full h-[500px] lg:h-[650px] group rounded-3xl overflow-hidden shadow-2xl border border-white/10" id="carousel-container">
                    
                    <!-- Slide 1: Infraestructura -->
                    <div class="carousel-slide absolute inset-0 opacity-100 transition-opacity duration-1000 ease-in-out z-10" data-index="0">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent z-10 opacity-60"></div>
                        <img 
                            src="/images/home1.png" 
                            alt="Infraestructura Propia Click Logística" 
                            class="w-full h-full object-cover"
                        />
                        <div class="absolute bottom-10 left-10 z-20">
                            <h3 class="text-3xl font-bold text-white mb-2 translate-y-0 transition-transform duration-700">Infraestructura Propia</h3>
                            <p class="text-brand-primary text-sm uppercase tracking-widest font-bold">Gestión Integral</p>
                        </div>
                    </div>

                    <!-- Slide 2: Tecnología -->
                    <div class="carousel-slide absolute inset-0 opacity-0 transition-opacity duration-1000 ease-in-out z-0" data-index="1">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent z-10 opacity-60"></div>
                        <img 
                            src="/images/home2.png" 
                            alt="Tecnología Aplicada Click Logística" 
                            class="w-full h-full object-cover"
                        />
                         <div class="absolute bottom-10 left-10 z-20">
                            <h3 class="text-3xl font-bold text-white mb-2 translate-y-0 transition-transform duration-700">Tecnología Aplicada</h3>
                            <p class="text-brand-primary text-sm uppercase tracking-widest font-bold">Innovación Constante</p>
                        </div>
                    </div>

                    <!-- Slide 3: Talento -->
                    <div class="carousel-slide absolute inset-0 opacity-0 transition-opacity duration-1000 ease-in-out z-0" data-index="2">
                        <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent z-10 opacity-60"></div>
                        <img 
                            src="/images/home3.png" 
                            alt="Profesionales de Élite Click Logística" 
                            class="w-full h-full object-cover"
                        />
                         <div class="absolute bottom-10 left-10 z-20">
                            <h3 class="text-3xl font-bold text-white mb-2 translate-y-0 transition-transform duration-700">Talento de Élite</h3>
                            <p class="text-brand-primary text-sm uppercase tracking-widest font-bold">Elegimos personas, no solo perfiles.</p>
                        </div>
                    </div>

                    <!-- Indicadores -->
                    <div class="absolute bottom-6 right-6 z-30 flex gap-2">
                        <button class="carousel-dot w-12 h-1 bg-brand-primary transition-all duration-300" onclick="goToSlide(0)" aria-label="Slide 1"></button>
                        <button class="carousel-dot w-6 h-1 bg-white/30 hover:bg-white transition-all duration-300" onclick="goToSlide(1)" aria-label="Slide 2"></button>
                        <button class="carousel-dot w-6 h-1 bg-white/30 hover:bg-white transition-all duration-300" onclick="goToSlide(2)" aria-label="Slide 3"></button>
                    </div>

                    <!-- Progressive Bar (Auto-play indicator) -->
                    <div class="absolute bottom-0 left-0 h-1 bg-brand-primary/50 z-40 transition-all duration-[5000ms] ease-linear w-0" id="progress-bar"></div>
                </div>

                <script>
                    let currentSlide = 0;
                    const slides = document.querySelectorAll('.carousel-slide');
                    const dots = document.querySelectorAll('.carousel-dot');
                    const progressBar = document.getElementById('progress-bar');
                    let interval;

                    function showSlide(index) {
                        slides.forEach((slide, i) => {
                            if (i === index) {
                                slide.classList.remove('opacity-0', 'z-0');
                                slide.classList.add('opacity-100', 'z-10');
                            } else {
                                slide.classList.remove('opacity-100', 'z-10');
                                slide.classList.add('opacity-0', 'z-0');
                            }
                        });

                        dots.forEach((dot, i) => {
                            if (i === index) {
                                dot.classList.remove('w-6', 'bg-white/30');
                                dot.classList.add('w-12', 'bg-brand-primary');
                            } else {
                                dot.classList.add('w-6', 'bg-white/30');
                                dot.classList.remove('w-12', 'bg-brand-primary');
                            }
                        });

                        // Reset Progress Bar
                        if(progressBar) {
                            progressBar.style.transition = 'none';
                            progressBar.style.width = '0%';
                            setTimeout(() => {
                                progressBar.style.transition = 'width 5000ms linear';
                                progressBar.style.width = '100%';
                            }, 50);
                        }
                        
                        currentSlide = index;
                    }

                    // Expose to window for onclick handlers
                    window.goToSlide = function(index) {
                        clearInterval(interval);
                        showSlide(index);
                        startAutoPlay();
                    }

                    function startAutoPlay() {
                        // Initial start of progress bar
                        if(progressBar) {
                             setTimeout(() => {
                                progressBar.style.transition = 'width 5000ms linear';
                                progressBar.style.width = '100%';
                            }, 50);
                        }

                        interval = setInterval(() => {
                            let next = (currentSlide + 1) % slides.length;
                            showSlide(next);
                        }, 5000);
                    }

                    // Start on load
                    document.addEventListener('DOMContentLoaded', startAutoPlay);
                </script>
            </div>
            
            <!-- Decorative Glows -->
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-brand-primary/10 rounded-full blur-[100px] -z-10"></div>
        </div>
            </div>
        </div>
    </section>

    <!-- Sección Reclutamiento (El Wizard Tecnológico) -->
    <section id="reclutamiento" class="py-32 bg-black relative overflow-hidden">
        <!-- Fondos decorativos -->
        <div class="absolute top-0 right-0 w-2/3 h-full bg-brand-primary/5 opacity-50 blur-[120px] pointer-events-none">
        </div>
        <div
            class="absolute bottom-0 left-0 w-2/3 h-full bg-brand-primary/5 opacity-30 blur-[120px] pointer-events-none">
        </div>

        <div class="max-w-4xl mx-auto px-6 relative z-10">
            <div class="text-center mb-16">
                <h2 class="text-4xl md:text-6xl font-black text-white mb-6 font-[Outfit]">TU FUTURO <span
                        class="text-brand-primary">EMPIEZA AQUÍ</span></h2>
                <p class="text-gray-400 text-lg">Postulación inteligente. 3 pasos. Sin complicaciones.</p>
            </div>

            <!-- Form Container -->
            <div
                class="glass-panel bg-brand-dark/50 border border-white/10 rounded-[2rem] p-8 md:p-14 shadow-2xl backdrop-blur-xl relative overflow-hidden">

                <!-- Decorative Top Border -->
                <div
                    class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-brand-primary to-transparent opacity-50">
                </div>

                <!-- Progress Bar -->
                <div class="flex items-center justify-between mb-12 relative px-4">
                    <div class="absolute left-0 top-1/2 -translate-y-1/2 w-full h-1 bg-white/10 rounded-full -z-10">
                    </div>
                    <div id="progress-line"
                        class="absolute left-0 top-1/2 -translate-y-1/2 h-1 bg-brand-primary rounded-full transition-all duration-700 ease-out"
                        style="width: 0%"></div>

                    <div class="step-dot active w-10 h-10 rounded-full bg-brand-primary flex items-center justify-center text-sm font-bold shadow-[0_0_15px_rgba(234,179,8,0.5)] transition-all z-10 text-black border-4 border-black"
                        data-step="1">1</div>
                    <div class="step-dot w-10 h-10 rounded-full bg-brand-dark border border-white/20 flex items-center justify-center text-sm font-bold transition-all z-10 text-white"
                        data-step="2">2</div>
                    <div class="step-dot w-10 h-10 rounded-full bg-brand-dark border border-white/20 flex items-center justify-center text-sm font-bold transition-all z-10 text-white"
                        data-step="3">3</div>
                </div>

                <!-- Wizard Content -->
                <form id="recruitment-form" onsubmit="event.preventDefault();">

                    <!-- Step 1: Info Básica -->
                    <div class="wizard-step" id="step-1">
                        <h3 class="text-2xl font-bold text-white mb-8">¿Quién eres?</h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                            <div class="space-y-3">
                                <label
                                    class="text-sm font-medium text-brand-primary ml-1 uppercase tracking-wide">Nombre
                                    Completo</label>
                                <input type="text" id="candidate-name"
                                    class="w-full bg-black/40 border border-white/10 rounded-xl px-5 py-4 text-white placeholder-gray-400 focus:border-brand-primary focus:ring-1 focus:ring-brand-primary focus:outline-none transition-all focus:bg-black/60"
                                    placeholder="Ej. Juan Pérez">
                            </div>
                            <div class="space-y-3">
                                <label
                                    class="text-sm font-medium text-brand-primary ml-1 uppercase tracking-wide">Correo
                                    Electrónico</label>
                                <input type="email" id="candidate-email"
                                    class="w-full bg-black/40 border border-white/10 rounded-xl px-5 py-4 text-white placeholder-gray-400 focus:border-brand-primary focus:ring-1 focus:ring-brand-primary focus:outline-none transition-all focus:bg-black/60"
                                    placeholder="juan@email.com">
                            </div>
                            <div class="space-y-3 md:col-span-2">
                                <label
                                    class="text-sm font-medium text-brand-primary ml-1 uppercase tracking-wide">Teléfono
                                    / WhatsApp</label>
                                <input type="tel" id="candidate-phone"
                                    class="w-full bg-black/40 border border-white/10 rounded-xl px-5 py-4 text-white placeholder-gray-400 focus:border-brand-primary focus:ring-1 focus:ring-brand-primary focus:outline-none transition-all focus:bg-black/60"
                                    placeholder="+57 300 000 0000">
                            </div>
                        </div>
                        <div class="mt-10 flex justify-end">
                            <button type="button" data-action="next" data-target="2"
                                class="px-10 py-4 bg-brand-primary hover:bg-yellow-400 text-black rounded-xl font-bold transition-all flex items-center gap-3 shadow-[0_4px_20px_rgba(234,179,8,0.3)] hover:shadow-[0_4px_30px_rgba(234,179,8,0.5)] transform hover:-translate-y-1">
                                Siguiente <i class="fa-solid fa-arrow-right text-sm"></i>
                            </button>
                        </div>
                    </div>

                    <!-- Step 2: Perfil -->
                    <div class="wizard-step hidden opacity-0 translate-x-10" id="step-2">
                        <h3 class="text-2xl font-bold text-white mb-8">Tu Perfil Profesional</h3>
                        <div class="space-y-8">
                            <div class="space-y-3">
                                <label class="text-sm font-medium text-brand-primary ml-1 uppercase tracking-wide">Área
                                    de Interés</label>
                                <div class="relative">
                                    <select id="candidate-area"
                                        class="w-full bg-black/40 border border-white/10 rounded-xl px-5 py-4 text-white focus:border-brand-primary focus:outline-none transition-all appearance-none cursor-pointer">
                                        <option>Operario Logístico</option>
                                        <option>Supervisor de Operaciones</option>
                                        <option>Conductor / Distribución</option>
                                        <option>Administrativo</option>
                                    </select>
                                    <div
                                        class="absolute right-5 top-1/2 -translate-y-1/2 text-brand-primary pointer-events-none">
                                        <i class="fa-solid fa-chevron-down"></i>
                                    </div>
                                </div>
                            </div>
                            <div class="space-y-3">
                                <label
                                    class="text-sm font-medium text-brand-primary ml-1 uppercase tracking-wide">Experiencia
                                    (Años)</label>
                                <div class="flex gap-4">
                                    <label class="flex-1 cursor-pointer">
                                        <input type="radio" name="exp" value="0-1" class="peer sr-only">
                                        <div
                                            class="text-center p-4 rounded-xl border border-white/10 hover:bg-white/5 peer-checked:bg-white peer-checked:text-black peer-checked:border-white peer-checked:font-bold transition-all">
                                            0-1
                                        </div>
                                    </label>
                                    <label class="flex-1 cursor-pointer">
                                        <input type="radio" name="exp" value="1-3" class="peer sr-only">
                                        <div
                                            class="text-center p-4 rounded-xl border border-white/10 hover:bg-white/5 peer-checked:bg-white peer-checked:text-black peer-checked:border-white peer-checked:font-bold transition-all">
                                            1-3
                                        </div>
                                    </label>
                                    <label class="flex-1 cursor-pointer">
                                        <input type="radio" name="exp" value="+3" class="peer sr-only">
                                        <div
                                            class="text-center p-4 rounded-xl border border-white/10 hover:bg-white/5 peer-checked:bg-white peer-checked:text-black peer-checked:border-white peer-checked:font-bold transition-all">
                                            +3
                                        </div>
                                    </label>
                                </div>
                            </div>
                        </div>
                        <div class="mt-10 flex justify-between">
                            <button type="button" data-action="prev" data-target="1"
                                class="px-8 py-4 text-gray-400 hover:text-white transition-colors font-medium">
                                Atrás
                            </button>
                            <button type="button" data-action="next" data-target="3"
                                class="px-10 py-4 bg-brand-primary hover:bg-yellow-400 text-black rounded-xl font-bold transition-all flex items-center gap-3 shadow-[0_4px_20px_rgba(234,179,8,0.3)] hover:shadow-[0_4px_30px_rgba(234,179,8,0.5)] transform hover:-translate-y-1">
                                Siguiente <i class="fa-solid fa-arrow-right text-sm"></i>
                            </button>
                        </div>
                    </div>

                    <!-- Step 3: CV & Confirmar -->
                    <div class="wizard-step hidden opacity-0 translate-x-10" id="step-3">
                        <h3 class="text-2xl font-bold text-white mb-8">Sube tu Hoja de Vida</h3>

                        <div
                            class="border-2 border-dashed border-white/20 rounded-2xl p-12 text-center hover:border-brand-primary hover:bg-white/5 transition-all cursor-pointer group">
                            <div
                                class="w-20 h-20 bg-white/5 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:bg-brand-primary group-hover:text-black transition-all duration-300 text-3xl text-gray-400">
                                <i class="fa-solid fa-cloud-arrow-up"></i>
                            </div>
                            <p class="font-bold text-lg text-white">Arrastra tu PDF aquí</p>
                            <p class="text-sm text-gray-500 mt-2">o haz clic para explorar tus archivos</p>
                        </div>

                        <div class="mt-6 flex items-center justify-center gap-2">
                            <input type="checkbox" id="terms-recruitment" required
                                class="w-5 h-5 rounded border-white/20 bg-white/5 text-brand-primary focus:ring-brand-primary/50 focus:ring-offset-0 cursor-pointer">
                            <label for="terms-recruitment" class="text-sm text-gray-400 select-none cursor-pointer">
                                Acepto los <button type="button" onclick="openTermsModal()"
                                    class="text-brand-primary hover:underline">Términos y
                                    Condiciones</button> y la <a href="/politica" target="_blank"
                                    class="text-brand-primary hover:underline">Política
                                    de Privacidad</a>
                            </label>
                        </div>

                        <div class="mt-10 flex justify-between items-center">
                            <button type="button" data-action="prev" data-target="2"
                                class="px-8 py-4 text-gray-400 hover:text-white transition-colors font-medium">
                                Atrás
                            </button>
                            <button type="button" data-action="finish"
                                class="px-10 py-4 bg-brand-primary hover:bg-yellow-400 rounded-xl font-bold text-black shadow-[0_4px_20px_rgba(234,179,8,0.3)] hover:shadow-[0_4px_30px_rgba(234,179,8,0.5)] transition-all flex items-center gap-3 transform hover:-translate-y-1">
                                Enviar Postulación <i class="fa-solid fa-check"></i>
                            </button>
                        </div>
                    </div>

                    <!-- Success Message -->
                    <div class="wizard-step hidden text-center py-10" id="step-success">
                        <div
                            class="w-24 h-24 bg-brand-primary rounded-full flex items-center justify-center mx-auto mb-8 text-5xl text-black shadow-[0_0_40px_rgba(234,179,8,0.6)] animate-bounce">
                            <i class="fa-solid fa-check"></i>
                        </div>
                        <h3 class="text-3xl font-bold text-white mb-4">¡Recibido!</h3>
                        <p class="text-gray-400 max-w-md mx-auto leading-relaxed">Tu perfil ya está en nuestra base de
                            datos inteligente. Si haces match con nuestras vacantes, te contactaremos en menos de 24h.
                        </p>
                        <button onclick="location.reload()"
                            class="mt-10 px-8 py-3 text-sm text-brand-primary border border-brand-primary/30 rounded-full hover:bg-brand-primary hover:text-black transition-all font-bold">
                            Volver al inicio
                        </button>
                    </div>

                </form>
            </div>
        </div>
    </section>

    <!-- Terms Modal -->
    <div id="termsModal" class="fixed inset-0 z-[10000] hidden">
        <div class="absolute inset-0 bg-black/80 backdrop-blur-sm" id="modalOverlay"></div>
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-2xl px-6">
            <div class="bg-brand-dark border border-white/10 rounded-2xl shadow-2xl p-8 max-h-[80vh] overflow-y-auto transform scale-95 opacity-0 transition-all duration-300" id="modalContent">
                <div class="flex justify-between items-start mb-6">
                    <h3 class="text-2xl font-bold text-white">Autorización de Tratamiento de Datos Personales</h3>
                    <button id="closeTermsBtn" class="text-gray-400 hover:text-white transition-colors">
                        <i class="fa-solid fa-xmark text-xl"></i>
                    </button>
                </div>
                <div class="prose prose-invert text-gray-300 text-sm leading-relaxed space-y-4">
                    <p>
                        De conformidad con la Ley 1581 de 2012 y el Decreto 1377 de 2013, informo que los datos
                        personales aquí consignados serán incorporados en una base de datos responsabilidad de
                        <strong>CLICK LOGÍSTICA</strong>, con la finalidad de:
                    </p>
                    <ul class="list-disc pl-5 space-y-2">
                        <li>Gestionar su postulación en procesos de selección actuales o futuros.</li>
                        <li>Contactarlo para entrevistas o pruebas psicotécnicas.</li>
                        <li>Enviarle información sobre vacantes que coincidan con sus intereses.</li>
                    </ul>
                    <p>
                        Como titular de los datos, usted tiene derecho a conocer, actualizar, rectificar y suprimir su
                        información, así como revocar esta autorización en cualquier momento. Puede consultar nuestra
                        Política de Tratamiento de Información en el siguiente enlace: <a href="#"
                            class="text-brand-primary hover:underline">Política de Privacidad</a> o escribirnos al
                        correo <a href="mailto:contacto@clicklogistica.com"
                            class="text-brand-primary hover:underline">contacto@clicklogistica.com</a>.
                    </p>
                </div>
                <div class="mt-8 pt-6 border-t border-white/10 flex justify-end">
                    <button id="acceptTermsBtn"
                        class="px-6 py-2 bg-brand-primary text-black font-bold rounded-lg hover:bg-yellow-400 transition-colors">
                        Acepto
                    </button>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Modal Logic
        const modal = document.getElementById('termsModal');
        const overlay = document.getElementById('modalOverlay');
        const closeBtn = document.getElementById('closeTermsBtn');
        const acceptBtn = document.getElementById('acceptTermsBtn');
        const modalContent = document.getElementById('modalContent');
        const check = document.getElementById('terms-recruitment');

        // Expose openTermsModal to global scope as it is called inline
        window.openTermsModal = function() {
            modal.classList.remove('hidden');
            setTimeout(() => {
                modalContent.classList.remove('opacity-0', 'scale-95');
                modalContent.classList.add('opacity-100', 'scale-100');
            }, 10);
        }

        function closeTermsModal() {
            modalContent.classList.remove('opacity-100', 'scale-100');
            modalContent.classList.add('opacity-0', 'scale-95');
            setTimeout(() => {
                modal.classList.add('hidden');
            }, 300);
        }

        function acceptTerms() {
            closeTermsModal();
            if (check) check.checked = true;
        }

        overlay.addEventListener('click', closeTermsModal);
        closeBtn.addEventListener('click', closeTermsModal);
        acceptBtn.addEventListener('click', acceptTerms);
    </script>
    <style>
        .hero-critical {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow: hidden;
            position: relative;
            background-color: #030303;
        }

        .h1-critical {
            font-family: 'Outfit', sans-serif;
            font-weight: 900;
            color: #ffffff;
            text-align: center;
            line-height: 1.1;
            font-size: 3rem;
            margin-bottom: 2rem;
        }

        .p-critical {
            font-family: 'Inter', sans-serif;
            font-weight: 300;
            color: #9ca3af;
            text-align: center;
            font-size: 1.25rem;
            max-width: 42rem;
            margin: 0 auto;
            line-height: 1.6;
        }

        @media(min-width: 768px) {
            .h1-critical {
                font-size: 6rem;
            }
        }
    </style>
</Layout>
