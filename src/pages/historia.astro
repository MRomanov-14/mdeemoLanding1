---
import Layout from '../layouts/Layout.astro';
---

<Layout title="CLICK Logística | Nuestra Historia">
    <!-- Hero Section -->
    <section class="relative min-h-[50vh] flex flex-col justify-center items-center overflow-hidden bg-[var(--color-bg)] pt-32 pb-20">
        <!-- Radial Gradient Overlay -->
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_var(--tw-gradient-stops))] from-brand-primary/20 via-transparent to-transparent pointer-events-none"></div>

        <div class="relative z-10 max-w-7xl mx-auto px-6 text-center">
            <h1 class="text-4xl md:text-6xl font-black tracking-tight text-[var(--color-text)] mb-6 leading-tight font-[Outfit] opacity-0 translate-y-8" id="hero-title">
                DE LA CONSULTORÍA<br />
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-600 via-amber-500 to-amber-600 dark:from-brand-primary dark:via-brand-primary/60 dark:to-brand-primary">A LA ACCIÓN</span>
            </h1>
            <p class="max-w-2xl mx-auto text-xl text-[var(--color-text-muted)] font-light leading-relaxed opacity-0 translate-y-4" id="hero-subtitle">
                Nuestra trayectoria: Evolucionando para ser el motor de tu éxito logístico.
            </p>
        </div>
    </section>

    <!-- History Content Section -->
    <section class="py-24 bg-[var(--color-bg)] relative overflow-hidden" id="history-grid">
        <div class="max-w-6xl mx-auto px-6 relative z-10">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-12">
                
                <!-- 2020: El Origen -->
                <div class="history-card group opacity-0 translate-y-12">
                    <div class="relative p-8 bg-white/5 dark:bg-[var(--color-surface)] border border-[var(--color-border)] rounded-3xl overflow-hidden hover:border-brand-primary/50 transition-all duration-500 hover:shadow-[0_20px_40px_rgba(0,0,0,0.1)] dark:hover:shadow-[0_20px_40px_rgba(0,0,0,0.3)] h-full">
                        <div class="absolute top-0 right-0 p-6 opacity-10 group-hover:opacity-20 transition-opacity">
                            <i class="fas fa-seedling text-6xl text-brand-primary"></i>
                        </div>
                        <div class="mb-6 inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-amber-100 dark:bg-brand-primary/10 text-amber-700 dark:text-brand-primary border border-amber-300 dark:border-brand-primary/20">
                            <span class="text-xl font-bold font-[Outfit]">2020</span>
                        </div>
                        <h3 class="text-2xl font-bold text-[var(--color-text)] mb-4 font-[Outfit]">El Origen</h3>
                        <p class="text-[var(--color-text-muted)] leading-relaxed font-light text-lg">
                            Iniciamos operaciones enfocados en fortalecer la cadena logística de las Pymes mediante consultoría integral y toma física de inventarios.
                        </p>
                    </div>
                </div>

                <!-- La Transformación -->
                <div class="history-card group opacity-0 translate-y-12" data-delay="0.2">
                    <div class="relative p-8 bg-white/5 dark:bg-[var(--color-surface)] border border-[var(--color-border)] rounded-3xl overflow-hidden hover:border-brand-primary/50 transition-all duration-500 hover:shadow-[0_20px_40px_rgba(0,0,0,0.1)] dark:hover:shadow-[0_20px_40px_rgba(0,0,0,0.3)] h-full">
                        <div class="absolute top-0 right-0 p-6 opacity-10 group-hover:opacity-20 transition-opacity">
                            <i class="fas fa-rocket text-6xl text-brand-primary"></i>
                        </div>
                        <div class="mb-6 inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-amber-100 dark:bg-brand-primary/10 text-amber-700 dark:text-brand-primary border border-amber-300 dark:border-brand-primary/20">
                            <i class="fas fa-bolt text-xl"></i>
                        </div>
                        <h3 class="text-2xl font-bold text-[var(--color-text)] mb-4 font-[Outfit]">La Transformación</h3>
                        <p class="text-[var(--color-text-muted)] leading-relaxed font-light text-lg">
                            Adaptándonos a la nueva realidad post-pandemia, evolucionamos de la teoría a la práctica. Identificamos la necesidad urgente de apoyo operativo en picos de demanda, especializándonos en picking, packing y gestión de contenedores.
                        </p>
                    </div>
                </div>

                <!-- Nuestro Diferenciador -->
                <div class="history-card group opacity-0 translate-y-12">
                    <div class="relative p-8 bg-white/5 dark:bg-[var(--color-surface)] border border-[var(--color-border)] rounded-3xl overflow-hidden hover:border-brand-primary/50 transition-all duration-500 hover:shadow-[0_20px_40px_rgba(0,0,0,0.1)] dark:hover:shadow-[0_20px_40px_rgba(0,0,0,0.3)] h-full">
                        <div class="absolute top-0 right-0 p-6 opacity-10 group-hover:opacity-20 transition-opacity">
                            <i class="fas fa-heart text-6xl text-brand-primary"></i>
                        </div>
                        <div class="mb-6 inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-amber-100 dark:bg-brand-primary/10 text-amber-700 dark:text-brand-primary border border-amber-300 dark:border-brand-primary/20">
                            <i class="fas fa-users-rays text-xl"></i>
                        </div>
                        <h3 class="text-2xl font-bold text-[var(--color-text)] mb-4 font-[Outfit]">Nuestro Diferenciador</h3>
                        <p class="text-[var(--color-text-muted)] leading-relaxed font-light text-lg">
                            Más allá de la logística, entregamos humanidad. Nos integramos a su operación con un ambiente de respeto y fraternidad, alineándonos con sus metas como un verdadero aliado estratégico.
                        </p>
                    </div>
                </div>

                <!-- Visión de Futuro -->
                <div class="history-card group opacity-0 translate-y-12" data-delay="0.2">
                    <div class="relative p-8 bg-brand-primary/5 dark:bg-brand-primary/5 border border-brand-primary/30 rounded-3xl overflow-hidden hover:border-brand-primary/60 transition-all duration-500 hover:shadow-[0_20px_40px_rgba(234,179,8,0.1)] h-full">
                        <div class="absolute top-0 right-0 p-6 opacity-10 group-hover:opacity-20 transition-opacity">
                            <i class="fas fa-eye text-6xl text-brand-primary"></i>
                        </div>
                        <div class="mb-6 inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-amber-100 dark:bg-brand-primary/20 text-amber-700 dark:text-brand-primary border border-amber-300 dark:border-brand-primary/40">
                            <i class="fas fa-compass text-xl"></i>
                        </div>
                        <h3 class="text-2xl font-bold text-[var(--color-text)] mb-4 font-[Outfit]">Visión de Futuro</h3>
                        <p class="text-[var(--color-text-muted)] leading-relaxed font-light text-lg">
                            Buscamos ser su primera opción, brindando tranquilidad y control mediante tecnología y métodos confiables.
                        </p>
                    </div>
                </div>

            </div>
        </div>
    </section>

    <!-- Final Quote Section -->
    <section class="py-20 relative overflow-hidden bg-[var(--color-bg)]">
        <div class="max-w-4xl mx-auto px-6 text-center relative z-10" id="quote-container">
            <div class="mb-8 text-brand-primary opacity-20 transform -translate-y-4 inline-block">
                <i class="fas fa-quote-left text-8xl"></i>
            </div>
            <h2 class="text-3xl md:text-5xl font-black text-[var(--color-text)] leading-tight font-[Outfit] mb-8 opacity-0" id="final-quote">
                "En CLICK Logística, no solo movemos productos,<br class="hidden md:block" /> movemos el éxito de tu empresa."
            </h2>
            <div class="w-24 h-1 bg-brand-primary mx-auto rounded-full scale-x-0" id="quote-divider"></div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-24 md:py-32 relative overflow-hidden bg-[var(--color-bg)]" id="cta-section">
        <div class="max-w-4xl mx-auto px-6 relative z-10">
            <!-- CTA Card -->
            <div class="relative opacity-0" id="cta-card">
                <!-- Thin accent line -->
                <div class="w-16 h-[2px] bg-amber-600 dark:bg-brand-primary mx-auto mb-16"></div>

                <!-- Stat Counters -->
                <div class="flex items-center justify-center gap-8 md:gap-16 mb-16" id="cta-stats">
                    <div class="text-center cta-stat opacity-0">
                        <div class="text-4xl md:text-6xl font-black text-[var(--color-text)] font-[Outfit] tracking-tight" data-count="5">0+</div>
                        <p class="text-xs text-[var(--color-text-muted)] uppercase tracking-[0.2em] mt-2">Años</p>
                    </div>
                    <div class="w-px h-12 bg-[var(--color-border)]"></div>
                    <div class="text-center cta-stat opacity-0">
                        <div class="text-4xl md:text-6xl font-black text-[var(--color-text)] font-[Outfit] tracking-tight" data-count="24">0+</div>
                        <p class="text-xs text-[var(--color-text-muted)] uppercase tracking-[0.2em] mt-2">Clientes</p>
                    </div>
                    <div class="w-px h-12 bg-[var(--color-border)]"></div>
                    <div class="text-center cta-stat opacity-0">
                        <div class="text-4xl md:text-6xl font-black text-[var(--color-text)] font-[Outfit] tracking-tight" data-count="1000">0+</div>
                        <p class="text-xs text-[var(--color-text-muted)] uppercase tracking-[0.2em] mt-2">Operaciones</p>
                    </div>
                </div>

                <!-- CTA Content -->
                <div class="text-center">
                    <h2 class="text-3xl md:text-4xl font-black text-[var(--color-text)] font-[Outfit] mb-4 leading-tight">
                        ¿Listo para escribir el <span class="text-amber-600 dark:text-brand-primary">próximo capítulo</span> juntos?
                    </h2>
                    <p class="text-[var(--color-text-muted)] text-lg font-light max-w-xl mx-auto mb-12 leading-relaxed">
                        Soluciones logísticas a la medida, respaldadas por experiencia y compromiso real.
                    </p>

                    <!-- Action Buttons -->
                    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                        <a href="/empresas"
                            class="px-8 py-3.5 bg-brand-primary text-black font-bold rounded-full transition-colors duration-200 hover:bg-yellow-400 flex items-center gap-3 text-base">
                            Solicita una Cotización
                            <i class="fa-solid fa-arrow-right text-sm"></i>
                        </a>
                        <a href="/empresas"
                            class="px-8 py-3.5 border border-[var(--color-border)] text-[var(--color-text)] font-medium rounded-full hover:border-brand-primary hover:text-brand-primary transition-colors duration-200 flex items-center gap-3 text-base">
                            Conoce Nuestros Servicios
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        import { gsap } from 'gsap';
        import { ScrollTrigger } from 'gsap/ScrollTrigger';

        gsap.registerPlugin(ScrollTrigger);

        // Hero Sequence
        const heroTl = gsap.timeline();
        heroTl.to('#hero-title', { 
            opacity: 1, 
            y: 0, 
            duration: 1, 
            ease: 'power3.out' 
        })
        .to('#hero-subtitle', { 
            opacity: 1, 
            y: 0, 
            duration: 0.8, 
            ease: 'power3.out' 
        }, '-=0.5');

        // Cards Stagger
        const cards = document.querySelectorAll('.history-card');
        cards.forEach((card) => {
            gsap.to(card, {
                scrollTrigger: {
                    trigger: card,
                    start: 'top 85%',
                    toggleActions: 'play none none none'
                },
                opacity: 1,
                y: 0,
                duration: 0.8,
                ease: 'power2.out'
            });
        });

        // Quote Animation
        gsap.timeline({
            scrollTrigger: {
                trigger: '#quote-container',
                start: 'top 80%'
            }
        })
        .to('#final-quote', {
            opacity: 1,
            duration: 1,
            ease: 'power2.out'
        })
        .to('#quote-divider', {
            scaleX: 1,
            duration: 1,
            ease: 'power4.inOut'
        }, '-=0.5');

        // CTA Fade In
        gsap.to('#cta-card', {
            scrollTrigger: {
                trigger: '#cta-section',
                start: 'top 75%'
            },
            opacity: 1,
            duration: 0.8,
            ease: 'power2.out'
        });

        // Stat Counters
        gsap.to('.cta-stat', {
            scrollTrigger: {
                trigger: '#cta-stats',
                start: 'top 85%'
            },
            opacity: 1,
            duration: 0.5,
            stagger: 0.1,
            ease: 'power2.out',
            onComplete: () => {
                document.querySelectorAll<HTMLElement>('[data-count]').forEach(el => {
                    const target = parseInt(el.dataset.count || '0');
                    const obj = { val: 0 };
                    gsap.to(obj, {
                        val: target,
                        duration: 1.5,
                        ease: 'power2.out',
                        onUpdate: () => {
                            el.textContent = Math.floor(obj.val).toLocaleString() + '+';
                        }
                    });
                });
            }
        });
    </script>
</Layout>
